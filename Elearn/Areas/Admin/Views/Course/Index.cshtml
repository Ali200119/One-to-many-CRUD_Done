@model IEnumerable<CourseListVM>

@{
    ViewBag.Title = "Course";
}

<div style="width: 70%; margin-left: 40px">
    <div style="display: flex; justify-content:right">
        <a asp-area="Admin" asp-controller="Course" asp-action="Create" style="color: white;background-color: green; border: none" class="btn my-5">Create</a>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th scope="col">Image</th>
                <th scope="col">Title</th>
                <th scope="col">Price</th>
                <th scope="col">Sales</th>
                <th scope="col">Operations</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var course in Model)
            {
                <tr>
                    <td><img src="~/images/@course.Image" /></td>
                    <td>@course.Title</td>
                    <td>$ @course.Price</td>
                    <td>@course.Sales</td>
                    <td>
                        <div style="display: flex">
                            <a asp-area="Admin" asp-controller="Course" asp-action="Details" asp-route-id="@course.Id" style="color: white;background-color: purple" class="btn">Details</a>
                            <a asp-area="Admin" asp-controller="Course" asp-action="Edit" asp-route-id="@course.Id" style="color: black; margin-left: 10px; background-color: yellow" class="btn">Edit</a>
                            <form method="post" asp-area="Admin" asp-controller="Course" asp-action="Delete" asp-route-id="@course.Id">
                                <button type="submit" style="color: black; margin-left: 10px; background-color: red" class="btn">Delete</button>
                            </form>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>